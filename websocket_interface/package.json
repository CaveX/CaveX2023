{
  "name": "websocket_interface",
  "version": "1.0.0",
  "description": "A Node.js app for Ingenuity 2023 which listens to LiDAR and obstacle data from ROS topics and then forwards them to an NGINX webserver over websockets. The data is then forwarded by the webserver to people on the website",
  "main": "app.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
	"dev": "nodemon app.ts"
  },
  "author": "Luka Moran (succinyl)",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "query-string": "^8.1.0",
    "ws": "^8.14.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.14",
    "@types/express": "^4.17.18",
    "@types/node": "^20.7.1",
    "@types/ws": "^8.5.6",
    "ts-node": "^10.9.1",
    "tsc": "^2.0.4",
    "typescript": "^5.2.2"
  }
}
